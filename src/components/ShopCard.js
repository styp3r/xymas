const ShopCard = (props) =>{
    return(
        <div id = "shopCard" style ={{width: props.width, height : props.height, margin: props.margin}}>
            
        </div>
    );
}

export default ShopCard;